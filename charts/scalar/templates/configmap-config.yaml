apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "scalar.labels" . | nindent 4 }}
data:
  config.json: {{ include "scalar.renderConfig" (dict "Values" .Values "tenant" dict) | quote }}
